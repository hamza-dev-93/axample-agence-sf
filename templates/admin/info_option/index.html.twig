{% extends 'base.html.twig' %}

{% block title %}InfoOption index
{% endblock %}

{% block body %}
    <h1>InfoOption index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
            {% for info_option in info_options %}
                <tr>
                    <td>{{ info_option.id }}</td>
                    <td>{{ info_option.name }}</td>
                    <td>
                        {# <a href="{{ path('info_option_show', {'id': info_option.id}) }}">show</a> #}
                        <a href="{{ path('info_option_edit', {'id': info_option.id}) }}">edit</a>

                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="3">no records found</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('info_option_new') }}">Create new</a>

{% endblock %}
