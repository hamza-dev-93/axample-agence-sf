{% extends 'base.html.twig' %}

{% block title %}InfoOption{% endblock %}

{% block body %}
    <h1>InfoOption</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ info_option.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ info_option.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('info_option_index') }}">back to list</a>

    <a href="{{ path('info_option_edit', {'id': info_option.id}) }}">edit</a>

    {{ include('info_option/_delete_form.html.twig') }}
{% endblock %}
