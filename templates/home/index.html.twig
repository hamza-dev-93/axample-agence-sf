{% extends 'base.html.twig' %}


{% block title %}
    Agence
{% endblock %}

{% block body %}
    <div class="container">
        <h1>Agence
        </h1>
        <div class="container">
            <h2>Les derniers biens</h2>
            <div class="row flex">
                {% for property in properties %}
                    <div class="col-3">
                        <div class="card">
                            <div class="card-body">
                                <h5 class="card-title">
                                    <a href="{{ path('property_show', {'id': property.id, 'slug': property.slug}) }} ">

                                        {{ property.title }}
                                    </a>
                                </h5>

                                <p class="cart-text">
                                    {{ property.city }}
                                    {{ property.postalCode }}
                                </p>
                                <div class="text-primary text-lg">{{ property.formattedPrice }}
                                    €
                                </div>


                            </div>
                        </div>
                    </div>

                </div>
            {% endfor %}
        </div>
    </div>
</div>{% endblock %}
